import FeedbackPopup from './ts/index';

// Export the main class and types
export { default as FeedbackPopup } from './ts/index';
export type { FeedbackPopupConfig } from './ts/index.d';

// Also make it available as default export for convenience
export default FeedbackPopup;
